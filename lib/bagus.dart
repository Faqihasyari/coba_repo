import 'package:flutter/material.dart';

class Bagus extends StatelessWidget {
  const Bagus({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Hello World'),
      ),
    );
  }
}